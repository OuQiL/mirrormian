# 基础设施：app（应用）+ milvus（向量库，etcd + MinIO + Milvus standalone）。
services:
  app:
    build: .
    environment:
      LLM_BASE_URL: ${LLM_BASE_URL:-https://api.openai.com/v1}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_MODEL: ${LLM_MODEL:-gpt-4o-mini}
      MIRROR_DB_PATH: /app/data/mirror-mian.db
      MILVUS_ADDR: milvus:19530
      MIRROR_KB_PATH: /app/kb
    volumes:
      - mian-data:/app/data
      - mian-kb:/app/kb
    depends_on:
      - milvus
    # 启动 Web 服务并固定端口 8012（供 cloudflared 隧道指向）
    command: ["web", "-addr", ":8012"]

  # Cloudflare Tunnel 旁车：把 8012 服务安全挂到你的域名，无需开放公网端口
  cloudflared:
    image: cloudflare/cloudflared:latest
    restart: unless-stopped
    command: ["tunnel", "--no-autoupdate", "run"]
    environment:
      # 在 .env 里填 CLOUDFLARE_TUNNEL_TOKEN=你的通道Token
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN:-}

  etcd:
    image: quay.io/coreos/etcd:v3.5.18
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - etcd-data:/etcd
    command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "endpoint", "health"]
      interval: 30s
      timeout: 20s
      retries: 3

  minio:
    image: minio/minio:RELEASE.2024-05-10T01-41-38Z
    environment:
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
    volumes:
      - minio-data:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  milvus:
    image: milvusdb/milvus:v2.4.1
    command: ["milvus", "run", "standalone"]
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
    volumes:
      - milvus-data:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    depends_on:
      - etcd
      - minio

volumes:
  mian-data:
  mian-kb:
  etcd-data:
  minio-data:
  milvus-data:
